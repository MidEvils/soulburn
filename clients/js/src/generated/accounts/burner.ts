/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
} from '@solana/kit';
import { BurnerSeeds, findBurnerPda } from '../pdas';
import { getKeyDecoder, getKeyEncoder, Key } from '../types';

export const BURNER_KEY = Key.Burner;

export function getBurnerKeyBytes() {
  return getKeyEncoder().encode(BURNER_KEY);
}

export type Burner = { key: Key; authority: Address; collection: Address };

export type BurnerArgs = { authority: Address; collection: Address };

export function getBurnerEncoder(): FixedSizeEncoder<BurnerArgs> {
  return transformEncoder(
    getStructEncoder([
      ['key', getKeyEncoder()],
      ['authority', getAddressEncoder()],
      ['collection', getAddressEncoder()],
    ]),
    (value) => ({ ...value, key: BURNER_KEY })
  );
}

export function getBurnerDecoder(): FixedSizeDecoder<Burner> {
  return getStructDecoder([
    ['key', getKeyDecoder()],
    ['authority', getAddressDecoder()],
    ['collection', getAddressDecoder()],
  ]);
}

export function getBurnerCodec(): FixedSizeCodec<BurnerArgs, Burner> {
  return combineCodec(getBurnerEncoder(), getBurnerDecoder());
}

export function decodeBurner<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Burner, TAddress>;
export function decodeBurner<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Burner, TAddress>;
export function decodeBurner<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Burner, TAddress> | MaybeAccount<Burner, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBurnerDecoder()
  );
}

export async function fetchBurner<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Burner, TAddress>> {
  const maybeAccount = await fetchMaybeBurner(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBurner<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Burner, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBurner(maybeAccount);
}

export async function fetchAllBurner(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Burner>[]> {
  const maybeAccounts = await fetchAllMaybeBurner(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBurner(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Burner>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeBurner(maybeAccount));
}

export function getBurnerSize(): number {
  return 65;
}

export async function fetchBurnerFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: BurnerSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {}
): Promise<Account<Burner>> {
  const maybeAccount = await fetchMaybeBurnerFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBurnerFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: BurnerSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {}
): Promise<MaybeAccount<Burner>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findBurnerPda(seeds, { programAddress });
  return await fetchMaybeBurner(rpc, address, fetchConfig);
}

/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  getStructDecoder,
  getStructEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
} from '@solana/kit';
import { CollectionSeeds, findCollectionPda } from '../pdas';
import { getKeyDecoder, getKeyEncoder, type Key, type KeyArgs } from '../types';

export type Collection = { key: Key };

export type CollectionArgs = { key: KeyArgs };

export function getCollectionEncoder(): FixedSizeEncoder<CollectionArgs> {
  return getStructEncoder([['key', getKeyEncoder()]]);
}

export function getCollectionDecoder(): FixedSizeDecoder<Collection> {
  return getStructDecoder([['key', getKeyDecoder()]]);
}

export function getCollectionCodec(): FixedSizeCodec<
  CollectionArgs,
  Collection
> {
  return combineCodec(getCollectionEncoder(), getCollectionDecoder());
}

export function decodeCollection<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Collection, TAddress>;
export function decodeCollection<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Collection, TAddress>;
export function decodeCollection<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Collection, TAddress> | MaybeAccount<Collection, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getCollectionDecoder()
  );
}

export async function fetchCollection<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Collection, TAddress>> {
  const maybeAccount = await fetchMaybeCollection(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCollection<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Collection, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeCollection(maybeAccount);
}

export async function fetchAllCollection(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Collection>[]> {
  const maybeAccounts = await fetchAllMaybeCollection(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeCollection(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Collection>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeCollection(maybeAccount));
}

export function getCollectionSize(): number {
  return 1;
}

export async function fetchCollectionFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: CollectionSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {}
): Promise<Account<Collection>> {
  const maybeAccount = await fetchMaybeCollectionFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCollectionFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: CollectionSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {}
): Promise<MaybeAccount<Collection>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findCollectionPda(seeds, { programAddress });
  return await fetchMaybeCollection(rpc, address, fetchConfig);
}
